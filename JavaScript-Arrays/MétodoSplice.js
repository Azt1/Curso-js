const nomes = ['Igor', 'alexandre', 'rosemary', 'nathalia'];

nomes.splice(1, 3);
console.log(nomes);


// nomes.splice(indice, delete, elem1, elem2)
// se eu quisesse colocar ate um numero maximo o comando é number.MAX_VALUE


// SIMULA O POP
// const removidos = nomes.splice(-1, 1);


//  SIMULA O SHIFT
// const removidos = nomes.splice(0, 1);
